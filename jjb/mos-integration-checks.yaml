- project:
    name: mos-integration-tests
    jobs:
      - 'check-{name}'

- job-template:
    name: 'check-{name}'
    project-type: matrix
    axes:
      - axis:
          type: tox
          values:
            - py27-static_check
            - py34-static_check
            - doc_check
            - testrail_id_check
    builders:
      - tox: {}

    scm:
      - git:
          url: http://172.17.0.1:8000/Mirantis/mos-integration-tests
          branches:
              - $GERRIT_BRANCH
          refspec: $GERRIT_REFSPEC
          choosing-strategy: gerrit
          skip-tag: true

    triggers:
      - gerrit:
          trigger-on:
            - patchset-created-event:
                exclude-drafts: true
                exclude-trivial-rebase: true
                exclude-no-code-change: true
            - comment-added-contains-event:
                comment-contains-value: 'recheck'
          projects:
            - project-compare-type: 'ANT'
              project-pattern: '**/mos-integration-tests'
              branches:
                - branch-compare-type: 'ANT'
                  branch-pattern: '**'
                # - branch-compare-type: 'PLAIN'
                #   branch-pattern: 'stable/8.0'
              # file-paths:
              #     - compare-type: ANT
              #       pattern: subdirectory/**
              # topics:
              #     - compare-type: ANT
              #       pattern: refactor-xy**
          # skip-vote:
          #     successful: true
          #     failed: true
          #     unstable: true
          #     notbuilt: true
          # silent: false
          # silent-start: true
          # escape-quotes: false
          # no-name-and-email: false
          # dependency-jobs: 'job1, job2'
          # notification-level: ALL
          # dynamic-trigger-enabled: true
          # dynamic-trigger-url: http://myhost/mytrigger
          # trigger-for-unreviewed-patches: true
          # server-name: __ANY__
          # failure-message-file: check.log
